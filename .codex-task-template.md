# Codex Task Template — Ghost Wave Theme

> Copy this file for each task (e.g., `tasks/01-home-intro.md`) and fill it out.

---

## 0. Context to read first

Required:
- codex-context.md
- ghost-blog-spec.md
- ghost-theme-implementation.md
- ghost-theme-file-map.md
- ghost-theme-dev-checklist.md

---

## 1. Task Title

(Short, imperative)

Example:
- Replace Home hero with site intro
- Add floating TOC on post page
- Implement series navigation

---

## 2. Scope

### In-scope
- (List what this task WILL change)

### Out-of-scope
- (List what this task MUST NOT change)

Hard rules:
- Work only in: `/mnt/d/workspace/02.works/ghost-theme-ayu`
- Preserve Wave theme structure and build pipeline
- Do not restructure templates/partials unless required by spec

---

## 3. Target Pages

- (Which pages must be visually/behaviorally affected?)

Example:
- `/` (Home)
- `/tag/{slug}/`
- `/{post-slug}/`
- `/categories/`
- `/series/`
- `/search/`

---

## 4. Acceptance Criteria (must be testable)

Write as checkboxes.

Example:
- [ ] Home hero cover is not shown
- [ ] Intro block is centered and 160–180px tall
- [ ] Latest posts list renders with pagination
- [ ] No console errors
- [ ] Mobile layout remains usable

---

## 5. File Plan (what files to touch)

### Expected to modify
- (List files)

### Expected to create
- (List new files)

### Must NOT touch
- (List files that must not change in this task)

---

## 6. Implementation Notes (spec mapping)

For each requirement, cite the spec section.

Example:
- Home layout: ghost-blog-spec.md → 3.1 Home
- Intro block: ghost-theme-implementation.md → Step 1–2
- CSS location: ghost-theme-repo-structure.md → CSS structure

---

## 7. Step-by-step Work Plan

Write exact steps Codex should follow:

1. Inspect current related templates/partials
2. Apply minimal changes
3. Update CSS/JS if needed
4. Run theme build pipeline (if present)
5. Package theme zip
6. Provide verification steps

---

## 8. Build & Packaging

### Build command (choose based on repo)
- (Fill in actual command once verified)
Examples:
- `npm ci && npm run build`
- `npm ci && npm run zip`
- `npm install && gulp build`
- `gulp zip`

Rules:
- Edit source files under `assets/css` and `assets/js`
- Regenerate `assets/built` via build pipeline
- Do not hand-edit `assets/built` unless no pipeline exists

### Output
- `wave-ayu-theme.zip`

---

## 9. Verification Plan (manual)

Use the running Ghost site:

- URL: `http://172.22.0.199:2368/`

List pages to check, and what to confirm.

Example:
- Home: ensure intro block appears; hero absent
- Tag page: ensure featured post policy correct
- Post page: ensure TOC hover works

---

## 10. Result Report (Codex must output)

When done, output a report in this exact structure:

### Summary
- (1–3 sentences)

### Files changed
- `path/to/file` — why changed
- ...

### Build/Packaging
- Command(s) run:
- Output zip path/name:

### Verification steps
- 1)
- 2)

### Checklist
- [ ] Task acceptance criteria satisfied
- [ ] No unintended template restructuring
- [ ] Build pipeline preserved
- [ ] Zip produced