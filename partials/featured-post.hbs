<article class="post featured-post {{post_class}}"{{#if og_description}} data-url="{{og_description}}"{{/if}} data-id="{{comment_id}}">
    <div class="post-media">
        {{#if og_description}}
            <div class="post-play js-play">
                {{> "icons/play"}}
                {{> "icons/pause"}}
            </div>
        {{/if}}
        <div class="u-placeholder rectangle">
            <a href="{{url}}">
                {{#if feature_image}}
                    <img class="post-image lazyload u-object-fit" src="{{img_url feature_image size="l"}}" alt="{{#if feature_image_alt}}{{feature_image_alt}}{{else}}{{title}}{{/if}}">
                {{else}}
                    <span class="letter">{{title}}</span>
                {{/if}}
            </a>
        </div>
    </div>

    <div class="post-wrapper">
        <header class="post-header">
            {{> "post-meta"}}
            <h2 class="post-title">
                <a class="post-title-link" href="{{url}}">{{title}}</a>
            </h2>
        </header>
        {{#if excerpt}}
            <div class="post-excerpt">{{excerpt words="40"}}</div>
        {{/if}}
    </div>
</article>
